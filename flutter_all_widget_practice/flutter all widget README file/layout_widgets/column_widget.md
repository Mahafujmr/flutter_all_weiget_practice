# Flutter Column Widget - গুরুত্বপূর্ণ টপিকস 📚

এই নোটে Flutter-এর **Column Widget** এর সবচেয়ে গুরুত্বপূর্ণ বিষয়গুলো সংক্ষেপে রাখা হয়েছে, যাতে পরে পড়াশোনা বা রেফারেন্সের জন্য ব্যবহার করতে পারো।

---

## ১. Column Widget কি?

* **Column** হলো Flutter-এর একটি **vertical layout widget**।
* এটি তার children (child widgets) **উপর থেকে নিচে** (vertical) সাজায়।
* Horizontal alignment নিয়ন্ত্রণ করা হয় `crossAxisAlignment` দিয়ে।

---

## ২. প্রধান Properties

| Property               | ব্যাখ্যা                                            | Default মান |
| ---------------------- | --------------------------------------------------- | ----------- |
| **children**           | Column-এর ভিতরের widget গুলোর list                  | -           |
| **mainAxisAlignment**  | Children কে vertical (উপর-নিচ)ভাবে align করে        | `start`     |
| **crossAxisAlignment** | Children কে horizontal (বাম/ডান/মধ্য)ভাবে align করে | `center`    |
| **mainAxisSize**       | Column কতটা vertical space নেবে তা নিয়ন্ত্রণ করে    | `max`       |
| **verticalDirection**  | Children কে vertical ভাবে কোন দিক থেকে সাজানো হবে   | `down`      |

---

## ৩. MainAxisAlignment মান এবং ব্যবহার

| Value          | ব্যাখ্যা                                                          |
| -------------- | ----------------------------------------------------------------- |
| `start`        | Column-এর উপরের দিকে children সাজাবে                              |
| `end`          | Column-এর নিচের দিকে children সাজাবে                              |
| `center`       | Column-এর মধ্যবিন্দুতে children রাখবে                             |
| `spaceBetween` | প্রথম এবং শেষ child দুই প্রান্তে, বাকি children এর মধ্যে সমান gap |
| `spaceAround`  | সব child-এর চারপাশে সমান space                                    |
| `spaceEvenly`  | সব child-এর মধ্যে একদম সমান দূরত্ব                                |

---

## ৪. CrossAxisAlignment মান এবং ব্যবহার

| Value     | ব্যাখ্যা                                         |
| --------- | ------------------------------------------------ |
| `start`   | সব child Column-এর বাম পাশে থাকবে                |
| `center`  | সব child Column-এর horizontal মধ্যবিন্দুতে থাকবে |
| `end`     | সব child Column-এর ডান পাশে থাকবে                |
| `stretch` | সব child Column-এর width পর্যন্ত প্রসারিত হবে    |

💡 **Important Note:**

* Column-এ crossAxisAlignment কাজ করার জন্য **Column-এর width থাকতে হবে**।
* যদি width auto হয় (children অনুযায়ী narrow হয়), তবে alignment effect দেখা যাবে না।
* Full width দিতে চাইলে `Container(width: double.infinity)` ব্যবহার করো।

---

## ৫. MainAxisSize

| Value | ব্যাখ্যা                                             |
| ----- | ---------------------------------------------------- |
| `max` | Column পুরো available vertical space দখল করবে        |
| `min` | Column শুধু তার children অনুযায়ী vertical space নেবে |

---

## ৬. Quick Tips

* Row vs Column shortcut:

  * **Row:** main axis = horizontal, cross axis = vertical
  * **Column:** main axis = vertical, cross axis = horizontal
* Container বা SizedBox দিয়ে width/height দিতে পারলে alignment effect দৃশ্যমান হয়।

---

এটি তোমার **Column Widget-এর quick reference** হিসেবে VS Code-এ রাখার জন্য পারফেক্ট।
