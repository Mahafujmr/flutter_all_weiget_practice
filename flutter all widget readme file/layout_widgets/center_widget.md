# Flutter Center Widget - গুরুত্বপূর্ণ টপিকস 📚

এই নোটে Flutter-এর **Center Widget** এর সবচেয়ে গুরুত্বপূর্ণ বিষয়গুলো সংক্ষেপে রাখা হয়েছে, যাতে পরে পড়াশোনা বা রেফারেন্সের জন্য ব্যবহার করতে পারো।

---

## ১. Center Widget কি?

* **Center** হলো Flutter-এর একটি **layout widget**, যা child widget কে **parent-এর ভিতরে কেন্দ্র (center) এ** রাখে।
* সাধারণভাবে horizontal এবং vertical দুই দিকেই alignment করে।

---

## ২. প্রধান Properties

| Property         | ব্যাখ্যা                                            | Default মান |
| ---------------- | --------------------------------------------------- | ----------- |
| **child**        | Center-এর ভিতরের একমাত্র widget                     | -           |
| **widthFactor**  | Child-এর width কে parent width-এর সঙ্গে scale করে   | null        |
| **heightFactor** | Child-এর height কে parent height-এর সঙ্গে scale করে | null        |

---

## ৩. Center Widget-এর ব্যবহার

```dart
Center(
  child: Container(
    width: 100,
    height: 100,
    color: Colors.red,
  ),
)
```

* এখানে Container parent এর মধ্যে **দুই দিকেই কেন্দ্র** এ অবস্থান করবে।

---

## ৪. WidthFactor এবং HeightFactor

* **widthFactor**:

  * যদি দেওয়া থাকে, Center widget-এর width হবে **child.width × widthFactor**
  * না থাকলে, parent যত জায়গা দিবে তা দখল করে
* **heightFactor**:

  * যদি দেওয়া থাকে, Center widget-এর height হবে **child.height × heightFactor**
  * না থাকলে, parent যত জায়গা দিবে তা দখল করে

---

## ৫. Key Notes

* Center widget শুধু **একটি child** নিতে পারে।
* Child কে horizontal ও vertical দুই দিকেই perfectly center-align করে।
* Row বা Column এর মধ্যে ব্যবহার করলে, child তখন parent container-এর ভিতরে centered হয়।
* Layout control সহজ করতে widthFactor ও heightFactor ব্যবহার করা যেতে পারে।

---

## ৬. Quick Tips

* যদি parent Container বা Scaffold থাকে, child সবসময় **মাঝখানে** থাকবে।
* যদি child এর width/height নির্দিষ্ট না থাকে, Center যত জায়গা পাবে, তার মধ্যে center করবে।
* অনেক সময় Row/Column এর ভিতরে ব্যবহার করা হয় ছোট widget কে center করার জন্য।

---

এটি তোমার **Center Widget-এর quick reference** হিসেবে VS Code-এ রাখার জন্য পারফেক্ট।
